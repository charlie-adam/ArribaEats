namespace ArribaEats.Models
{
    /// <summary>
    /// Represents the role of a user in the system.
    /// </summary>
    public enum UserRole
    {
        Customer,
        Deliverer,
        Client
    }
}